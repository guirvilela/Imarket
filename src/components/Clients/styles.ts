import styled from 'styled-components';

export const Container = styled.main`
  h1 {
    font-size: 1.3rem;
    font-weight: 400;
    color: var(--info-text);
  }

  p {
    color: var(--sub-title);
    margin: 1.5rem 0;
    line-height: 1.5;
  }
`;
